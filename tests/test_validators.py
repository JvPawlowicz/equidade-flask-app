import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('', (), {'data': '+55 11 99999-9999'})) is None
        
    def test_validate_phone_invalid(self):
        with pytest.raises(ValidationError):
            Validators.validate_phone(None, type('', (), {'data': 'invalid'}))
import pytest
from security.validators import Validators

class TestValidators:
    def test_validate_phone_valid(self):
        assert Validators.validate_phone(None, type('',